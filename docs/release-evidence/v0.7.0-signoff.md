# v0.7.0 Release Evidence

Date: 2026-06-05

## UI acceptance target

- Approved visual target: `docs/design/v0.7.0/approved-ui-direction.png`
- Implemented frontend: `src/meeting_recorder/modern_ui.py`
- Screenshot command: `meeting-recorder gui-screenshot --output dist/meeting-recorder-gui-v0.7.0.png`

## Expected gates

- Unit tests: `python3 -m pytest -q`
- Compile check: `python3 -m compileall src scripts`
- Screenshot smoke: `PYTHONPATH=src python3 -m meeting_recorder.cli gui-screenshot --output dist/meeting-recorder-gui-v0.7.0.png`
- Release assets: `python3 scripts/build_release_assets.py`
- Checksums: `(cd dist && sha256sum -c SHA256SUMS)`

## Notes

The v0.7.0 screenshot renderer uses the same approved HTML/CSS as the runtime UI and requires an Electron/Chromium-compatible renderer. It fails if no renderer exists rather than producing a fallback evidence card.

## Signoff

- Engineer signoff: modern WebView-style UI implemented, CLI routed to modern launcher/screenshot path, local tests/build/checksums passed.
- QA/research signoff: accepted after independent release-gate review; screenshot artifact matches the approved visual target dimensions and includes tray/topbar, glass recorder panel, waveform, Pause/Stop/Resume controls, settings rows, and save/transcribe panel.
